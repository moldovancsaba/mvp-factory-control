/**
 * Next.js config for `apps/mvp-factory-control`. App Router; experimental flags added only when required.
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  experimental: {
    // Keep defaults; add flags intentionally when needed.
  },
};

export default nextConfig;

